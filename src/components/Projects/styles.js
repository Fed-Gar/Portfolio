import makeStyles from "@material-ui/core/styles/makeStyles";

export const useStyles = makeStyles((theme) => ({
	root: {
		marginTop: 100,
  },
	title: {
    marginLeft: '4rem',
		marginBottom: '1rem',
  },
}));